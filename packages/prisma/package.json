{
  "name": "@packages/prisma",
  "version": "0.2.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "generate": "yarn prisma generate",
    "studio": "yarn prisma studio",
    "migrate": "yarn prisma migrate --experimental",
    "seed": "ts-node -T ./seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^2.9.0"
  },
  "devDependencies": {
    "@prisma/cli": "^2.9.0"
  }
}