{
  "name": "@server/prisma",
  "version": "0.3.4",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "nodemon -q -w schema.prisma -x yarn migrate dev",
    "generate": "yarn prisma generate",
    "migrate": "yarn prisma migrate --preview-feature",
    "studio": "yarn prisma studio"
  },
  "dependencies": {
    "@prisma/cli": "^2.15.0",
    "@prisma/client": "^2.15.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
